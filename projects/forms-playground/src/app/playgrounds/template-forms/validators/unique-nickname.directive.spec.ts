import { UniqueNicknameDirective } from './unique-nickname.directive';

describe('UniqueNicknameDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueNicknameDirective();
    expect(directive).toBeTruthy();
  });
});
