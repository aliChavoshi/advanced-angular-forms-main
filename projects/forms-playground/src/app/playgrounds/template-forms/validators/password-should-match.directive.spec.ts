import { PasswordShouldMatchDirective } from './password-should-match.directive';

describe('PasswordShouldMatchDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordShouldMatchDirective();
    expect(directive).toBeTruthy();
  });
});
